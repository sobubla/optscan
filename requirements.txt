fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.2
fyers-apiv3>=3.1.7
python-dotenv>=1.0.1
vollib>=1.0.11
optionlab>=1.6.0
# requests is also a fyers-apiv3 transitive dep; list it explicitly because
# openalgo_client.py imports it directly. No version pin to avoid conflicts.
requests
numpy>=2.0
pandas>=2.0
pytest>=8.0
